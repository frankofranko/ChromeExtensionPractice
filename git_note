
git clone http_url

git branch

git status

git add .
git add filename

git commit

git push origin branch_name
git push -u origin branch_name (save origin branch_name)

git checkout -b new_branch_name

git pull origin master

git config --global alias.co checkout
